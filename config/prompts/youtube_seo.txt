Create YouTube SEO for **Doodlytical** — US psychology stick-figure channel (~15 min).

Topic: {topic}

**LOCKED TITLE (use exactly — do not change a single character):**
{locked_title}

Past videos (differentiate in description/tags only):
{past_topics}

Output ONLY valid JSON (no markdown, no backticks):
{
  "title": "{locked_title}",
  "description": "MINIMUM 600 characters. (1) hook paragraph — why watch, (2) 3-5 bullets of what they learn, (3) 'Timestamps coming soon', (4) subscribe CTA. Plain text.",
  "tags": ["tag1", ... max 15, US search intent],
  "hashtags": ["#tag1", ... max 5]
}

Rules:
- `title` field MUST equal the locked title exactly
- Description: speak to viewer's situation; mention Doodlytical once naturally
- Tags: specific mechanisms (procrastination, hypervigilance, etc.) not generic "mindset"
- No ALL CAPS title, no emoji spam
